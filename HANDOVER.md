# 행정 담당자 운영·이관 안내

기본 운영은 Google Sheet가 아닌 포털의 관리자 메뉴에서 합니다. Sheet는 비상 확인·복구용 운영 DB입니다.

## 학생·계정 관리

- 학생 추가: `근로학생 관리 → 학생 추가`에서 이름, 학번, 파트, 근로유형, 기간, 연락처, 개별 시급을 저장합니다. 초기 ID와 비밀번호는 학번으로 자동 생성됩니다.
- 정보 수정: 해당 행의 `수정`을 누릅니다. 파트·유형·기간·업무도 같은 화면에서 바꿉니다.
- 비활성화: 행을 지우지 말고 `비활성화`를 누릅니다. 로그인이 즉시 차단되고 과거 기록은 남습니다.
- 비밀번호 초기화: 해당 행의 `학번으로 초기화`를 누릅니다. 저장되는 값은 평문이 아닌 salt 해시입니다.
- 단기근로: 근로유형 `SHORT_TERM`을 선택하고 시작일·종료일을 모두 입력합니다. 종료 후 로그인과 출근은 차단하지만 계정과 과거 기록은 삭제하지 않습니다.
- 관리자 계정: `계정 관리`는 SUPER_ADMIN에게만 보입니다. MANAGER 발급·권한 변경·임시 비밀번호 초기화·비활성화·삭제 처리가 가능하며 마지막 SUPER_ADMIN 비활성화/삭제는 서버가 차단합니다. 삭제 처리는 운영 감사를 위해 행을 지우지 않고 로그인 차단·삭제자·삭제시각을 기록합니다.
- 본인 비밀번호: 학생과 관리자 모두 `내 정보/내 계정`에서 현재 비밀번호 검증 후 변경합니다.
- 학생 고유색: 학생 수정의 색상 입력값이 오늘·주간·월간 스케줄 구분색으로 사용됩니다.

## 시간표·근무기록

- 시간표: `주간 시간표`에서 조직·근로유형·학생별 주간/월간표를 확인합니다. 블록을 누르면 바로 수정할 수 있고 `시간표 편집 → 시간 추가`에서 반복 일정 또는 특정 날짜 1회 일정을 만듭니다. 30분 단위만 저장되며 삭제는 비활성화 방식입니다.
- 기록 조회: `근무기록 관리`에서 학기·월·파트·학생·상태로 필터합니다.
- 직접 추가: `근무기록 직접 추가`에서 학생, 날짜, 출·퇴근, 사유를 입력합니다.
- 보정: 해당 기록의 `수정`을 누릅니다. 인정분은 자동 재계산되고 수정자·수정시각이 남습니다.
- `확인 필요`: 퇴근 누락, 중복, 시각 역전, 비활성 학생, 시간표 불일치는 자동 삭제/보정하지 말고 담당자가 확인합니다.
- 결근: `오늘 근무 → 결근 처리`에서 유형·사유를 등록합니다. 예정시간은 유지되고 실제시간/실제 인건비는 0이며, 수정·취소 이력이 별도 `Absences` 시트에 남습니다.

## 학기·파트·업무

- 새 학기: `학기 관리 → 새 학기`에서 ID, 이름, 기간을 입력합니다.
- 활성 학기: 해당 학기의 `활성화`를 누릅니다. 코드 수정이나 Apps Script 재배포가 필요 없습니다. 과거 학생·기록은 삭제되지 않습니다.
- 종강/방학: 운영 종료일은 2027-02-28까지 유지하고, 실제 종강일에 `종강·방학 시작`을 눌러 그날부터 `VACATION` 시간표를 적용합니다. 예상일(12월 22일 전후)을 자동 확정하지 않습니다.
- 조직: 기본 `SUPPORT`(지원팀), `RESERVE`(예비군연대)입니다. 단기근로는 조직이 아니라 학생의 근로유형입니다. `파트 관리`에서 새 조직 추가·수정·미사용 조직 비활성화를 할 수 있습니다.
- 담당업무: `담당업무`에서 추가·수정·비활성화합니다. 관리자 메모는 학생 위키 응답에 포함되지 않습니다.
- 예산: `예산 → 예산 입력·수정`에서 전체·국가근로·교내근로·단기근로 월 예산과 메모를 저장합니다. 소속이 아니라 기존 `workerType`으로 집계하며 예정·실제 인건비와 두 잔여액이 표시됩니다. 빈 값은 `예산 미설정`으로 보이고 시급은 학생 개별값 → 기본 10,320원 순입니다.

## 인수인계

- 학생은 모바일 `인수인계`에서 새 글을 작성하고 전체 공개 글을 확인할 수 있습니다. 본인 글만 수정·삭제·완료할 수 있으며 다른 사람 글은 `확인`만 남깁니다.
- 관리자는 전체 글을 수정·중요 표시·상단 고정·완료·삭제 처리하거나 작성 학생이 없는 공지성 공유메모를 등록합니다.
- 삭제 처리는 행을 제거하지 않고 `active=false`, 삭제자·삭제시각을 남깁니다.

## 공지·소집

- 공지: SUPER_ADMIN은 전체 공지 관리, MANAGER는 작성 및 본인 공지 수정·삭제가 가능합니다. 학생 홈에는 고정 공지를 먼저 표시합니다.
- 소집: 관리자 생성 시 자율 신청/관리자 지정, 대상, 정원, 장소, 근로시간 인정 여부를 정합니다. 학생은 자율 신청 소집을 신청·취소할 수 있습니다.
- 소집 생성만으로 시간은 지급되지 않습니다. 근로시간 인정 소집도 실제 출퇴근 또는 관리자 참석완료 후에만 WorkLogs에 반영하며 기존 기록과 중복시간을 차단합니다.

## 대체근무

학생이 본인 일정으로 요청하면 같은 `partId`의 학생에게만 보입니다. 다른 파트는 UI 후보·API 응답·신청·관리자 승인의 모든 단계에서 차단됩니다.

## 담당자 이관

1. Google Sheet `공유`에서 새 한양대 담당자를 편집자로 추가합니다. 정책상 가능하면 소유권을 부서 계정으로 이전합니다.
2. 새 담당자용 일반 관리자 ID/PW를 `Admins`에 salt 해시 방식으로 발급하고 실제 로그인을 확인한 뒤 이전 관리자 행을 비활성화합니다.
3. 스프레드시트에 연결된 Apps Script 프로젝트를 새 담당자가 열고 배포를 관리할 수 있는지 확인합니다.
4. GitHub `Settings → Collaborators and teams`에 계정을 추가합니다. 소유권 이전은 `Settings → General → Transfer ownership`에서 합니다.

## Apps Script 재배포

`Apps Script → 배포 → 배포 관리 → 공개 운영 API 배포 → 수정 → 새 버전 → 배포` 순서입니다. 기존 배포를 편집하면 URL은 유지됩니다.
